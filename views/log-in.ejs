<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Log in</title>
	<link rel="stylesheet" href="/styles/index.css">
	<link rel="stylesheet" href="/styles/authenticationForm.css">
</head>

<body class="authenticationFormPage">
	<div class="authenticationFormContainer">
		<form action="/log-in" method="post" class="authenticationForm">
			<h1>Log in</h1>

			<div class="authenticationFormField">
				<label for="username">Username</label>
				<input type="text" name="username" id="username">
			</div>

			<div class="authenticationFormField">
				<label for="password">Password</label>
				<input type="password" name="password" id="password">
			</div>
		
			<% if (locals.messages && locals.messages.length > 0) { %>
				<ul class="error-list">
					<% messages.forEach(msg => { %>
						<li><%= msg %></li>
					<% }) %>
				</ul>
			<% } %>

			<p>Don't have an account? <a href="/sign-up">Sign up</a></p>

			<button type="submit">Log in</button>
		</form>

	</div>
</body>

</html>